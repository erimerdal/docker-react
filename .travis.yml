language: generic 

sudo: required
services:
  - docker

before_install:
  - docker build -t erimerdal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true erimerdal/docker-react npm run test